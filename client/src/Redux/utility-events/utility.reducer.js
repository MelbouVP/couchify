import utilityActionTypes from './utility.types'

const INITIAL_STATE = {
    filterSectionHidden: false
}

const utilityEventsReducer = (state=INITIAL_STATE, action) => {
    switch(action.type){
        default:
            return state
    }

}

export default utilityEventsReducer;